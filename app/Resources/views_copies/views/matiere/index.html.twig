{% extends 'adminbase.html.twig' %}

{% block admin %}
    <h1>Matieres list</h1>
    <ul>
          <a href="{{ path('matiere_new') }}" role="button" class="btn btn-primary" > Ajouter</a>
        
    </ul>

    <table border="1"; style="width:auto";>
        <thead border="1" ; bgcolor="#00FF00">
            <tr>
                <th>Id</th>
                <th>Nom matiere</th>
                <th width="auto">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for matiere in matieres %}
            <tr>
                <td><a href="{{ path('matiere_show', { 'id': matiere.id }) }}">{{ matiere.id }}</a></td>
                <td>{{ matiere.nomMatiere }}</td>
                
                <td bgcolor="#BBD2E1";>
                    
                    <ul>
                        <!--input type="submit" value="Delete"-->
                            <a href="{{ path('matiere_show', { 'id': matiere.id }) }}" type="submit"  class="btn btn-primary btn-sm">show</a> &nbsp&nbsp&nbsp
                            <a></a>
<a></a>
                            <a href="{{ path('matiere_edit', { 'id': matiere.id }) }}" class="btn btn-primary btn-sm" >edit</a>

                            &nbsp &nbsp &nbsp 

                            <a href="{{ path('matiere_delete', { 'id': matiere.id }) }}" class="btn btn-danger btn-sm">Delete</a>
                                                
                    </ul>
                
                    
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('matiere_new') }}">Create a new matiere</a>
        </li>
    </ul>
{% endblock %}
