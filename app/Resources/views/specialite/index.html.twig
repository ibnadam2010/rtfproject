{% extends 'adminbase.html.twig' %}

{% block admin %}
    {{ render(controller('AppBundle:Specialite:new')) }}
    <br><br><br>
    <h3>Liste des specialités</h3>
    <div class="form-group">
        <div class = "input-group">
            <input type="text" id ="filter" name = "prenom" class="form-control" placeholder="Rechercher une specialité"/>
        </div>
    </div>
<table  class="footable" data-page-size ="10" data-filter="#filter">
    <thead >
    <th data-toggle="true" style="background:#122e48">Id</th>
    <th data-hide="phone" style="background:#122e48">Specialité</th>
    <th data-hide="phone" style="background:#122e48">Action</th>
    </thead>
    <tbody >
    {% for specialite in specialites %}
    <tr>
        <td>{{ specialite.id }}</td>
        <td>{{ specialite.nomSpecialite }}</td>
        <td>
            {{ render(controller('AppBundle:Specialite:edit',{'id':specialite.id})) }}
            <button class="btn btn-danger btn-sm confirm"  onclick="deleteDep('{{ specialite.id }}')" ><i class="fas fa-trash-alt"></i></button>
        </td>
    </tr>
    {% endfor %}
    </tbody>
    <tfoot class = "hide-if-no-paging">
    <td colspan="3">

        <div class = "pagination">

        </div>

    </td>

    </tfoot>
</table>


    <script type="text/javascript">
        $(".footable").footable();
    </script>
{% endblock %}
